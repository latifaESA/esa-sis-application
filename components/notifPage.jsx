import React from "react";
import NotificationCards from "./notificationCards";

const NotifPage = () => {
  return (
    <div>
      <NotificationCards />
      <NotificationCards />
      <NotificationCards />
      <NotificationCards />
      <NotificationCards />
    </div>
  );
};

export default NotifPage;
